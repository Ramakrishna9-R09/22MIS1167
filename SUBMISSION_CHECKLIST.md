# Submission Checklist

- Repository name is `22MIS1167`.
- Backend track folder structure is present.
- Logging middleware is implemented and integrated.
- Vehicle maintenance scheduler exposes a local API and includes output screenshots.
- Notification system design covers all six stages.
- Priority inbox backend exposes a local API and includes output screenshots.
- Secrets are read from environment variables and are not committed.
- Local request collection is included for quick API verification.
- Package-level syntax check scripts are available for the runnable backend services.
